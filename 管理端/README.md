# 管理端

无需构建工具，直接打开 `index.html`。页面连接 `http://localhost:8080/api/admin`，需要先通过环境变量初始化管理员并启动后端。

当前已实现：

- 管理员登录、JWT 保存和退出；
- 最近 30 天订单、实收金额、出票量和核销量指标；
- 最近订单列表；
- 开放日、场次、票种和库存列表；
- 订单、支付和核销记录筛选查询；
- 响应式桌面和移动端布局。

下一阶段补齐票务资源新增/编辑弹窗、操作反馈和细粒度角色权限。
