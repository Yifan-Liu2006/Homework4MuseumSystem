# Homework4MuseumSystem

博物馆订票系统数据库课程设计项目。系统围绕游客预约、票务库存、在线支付、电子凭证、入园核销和后台运营管理展开。

## 项目结构

```text
.
├── README.md
├── 数据库层/
│   ├── museum_ticket_schema.sql       # MySQL 数据库结构与核心事务过程
│   ├── E-R图.png                       # 实体-联系模型
│   ├── 大作业数据流图.png                # 游客端/管理端数据流
│   ├── 博物馆订票系统数据库结构设计.docx  # 数据库设计说明
│   └── 作业要求.docx                    # 课程设计要求
└── 上层程序/                            # 应用层代码（待实现）
```

## 核心功能

- 游客注册、登录和实名参观人员管理
- 开放日期、参观场次、票种和库存管理
- 预约下单、支付、取消、过期和退款
- 电子入园凭证生成与二维码核销
- 后台订单、支付、核销、异常和统计管理
- 管理员、角色权限和操作日志

## 数据库

数据库使用 MySQL 8.0+，默认数据库名为 `museum_ticket`。执行脚本：

```sql
SOURCE /absolute/path/to/数据库层/museum_ticket_schema.sql;
```

脚本包含：

- 游客、实名人员、开放日期、场次、票种、库存、订单和订单明细表；
- 支付记录、电子凭证、核销记录、管理员、角色和操作日志表；
- 主键、外键、唯一约束和金额/库存检查约束；
- 带行锁、重复预约检查和异常回滚的 `create_ticket_order` 存储过程。

## 业务状态

- 订单：待支付、已支付、已取消、已退款、已过期
- 支付：未支付、已支付、已退款
- 凭证：有效、已使用、已作废、已过期
- 核验：未核验、已核验、已作废

## 当前状态

目前已完成数据库层设计和修正版 MySQL 脚本；`上层程序/` 尚未实现。当前支付可以先采用模拟支付，后续再接入微信支付或支付宝。

## 后续开发计划

1. 选择后端技术栈并建立数据库连接。
2. 实现游客、实名人员、日期/场次、库存和订单 API。
3. 实现事务下单、支付回调、取消订单和退款逻辑。
4. 实现电子凭证、二维码和后台扫码核销。
5. 实现管理端票务配置、异常处理、统计、角色权限和日志。
6. 增加定时任务、并发购票测试、安全加固和部署配置。

## 安全注意事项

- 生产环境只保存密码哈希，不保存明文密码。
- 证件号码应保存哈希或加密值，并在界面中脱敏显示。
- 支付回调必须验证签名并保证幂等。
- 游客只能访问属于自己的订单和实名信息。
- 数据库账号应使用最小权限，生产环境不要使用示例密码。

## 许可证

本项目用于数据库课程设计和学习。
